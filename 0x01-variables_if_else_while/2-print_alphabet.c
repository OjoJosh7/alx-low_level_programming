#include <stdio.h>
/**
 * main - It initiates the code in the block
 * Return: always 0 (success)
 */

int main(void)
{
char a;

for (y = 'a'; y <= 'z'; y++)
	putchar(y);
putchar('\n');
return (0);
}
