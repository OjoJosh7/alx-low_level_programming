#include<stdio.h>

/**
 * main - it initiates the code block
 * printf: displays a text
 * Return: always 0 (success)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
