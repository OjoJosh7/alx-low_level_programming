#include "main.h"
/**
 * _strcat - it is a fuction that concatenates two strings
 * @dest: Destination to be appended to
 * @src: string to be appended
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	for (n = 0; (dest[i + n] = *src++) != '\0'; n++)
	{
	}
	return (dest);
}
